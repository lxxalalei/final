"""shared — 平台搜索层共享的 Python 工具包。

包含以下模块：
- logger.getLogger — 统一日志
- platform_base — 平台 Skill 基类（CLIBasedPlatformSkill）
- config_loader — 统一配置加载器（ConfigLoader / get_config）
- dedup — 跨平台内容级去重引擎（DedupEngine / DedupConfig）
"""
